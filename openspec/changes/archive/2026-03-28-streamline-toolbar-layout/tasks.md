## 1. 工具栏结构重组

- [x] 1.1 重排 `index.html` 中单层 toolbar 的控件顺序，形成“身份 / 工作区控制 / 输出动作”主流程
- [x] 1.2 弱化 GitHub / Star 入口的结构表达（如图标化或紧凑化），避免其与主操作同权
- [x] 1.3 为未来 `Split / Preview` 视图模式预留稳定入口位置
- [x] 1.4 将 `status` 从主工具栏控件流中移出到独立反馈区域

## 2. 视觉层级与空间压缩

- [x] 2.1 调整 `styles.css` 中 toolbar 的横向布局规则，降低默认拥挤感
- [x] 2.2 将 `Generate DOCX` 设为唯一主按钮，保留 `Print Preview` 为次按钮
- [x] 2.3 降低低频控件（语言、外链）的视觉权重
- [x] 2.4 优化字体选择器与其他控件的间距和尺寸，使单层布局更稳定

## 3. 状态反馈与文案适配

- [x] 3.1 调整状态反馈容器的位置与样式，避免打断主控制流
- [x] 3.2 更新相关 i18n 文案，确保单层工具栏新结构在中英文下都成立
- [x] 3.3 如有必要，为未来视图模式入口添加占位文案或标签

## 4. 响应式与稳定性验证

- [ ] 4.1 验证常见桌面宽度下 toolbar 尽量保持单行稳定显示
- [ ] 4.2 定义窄宽度下的优先级策略，保证主工作流控件优先可见
- [ ] 4.3 验证工具栏整理后不会明显压缩编辑区和预览区的有效高度

## 5. 手动测试与文档

- [ ] 5.1 手动测试导出、打印、字体切换、图表配置、语言切换入口是否正常工作
- [ ] 5.2 手动测试状态反馈在成功、失败、加载场景下的可见性与不打断性
- [x] 5.3 更新 `readme.md` 或相关说明（如有必要）以反映工具栏结构调整
