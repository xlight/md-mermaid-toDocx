## 1. 工作区模式与状态管理

- [x] 1.1 在 `app.js` 中定义工作区模式状态：`split` / `editor-only` / `preview-only`
- [x] 1.2 增加会话内 `lastSplitRatio` 状态（仅内存，不落盘）
- [x] 1.3 实现统一的 `applyWorkspaceMode()`，负责切换布局与控件显隐
- [x] 1.4 在模式切换时保持入口状态同步（按钮激活态/可用态）

## 2. 视图模式入口与结构调整

- [x] 2.1 在 `index.html` 将占位视图入口替换为可操作的模式切换控件
- [x] 2.2 为分割线添加独立 DOM 节点（仅 `split` 模式显示）
- [x] 2.3 在 `styles.css` 补充分割线视觉与拖拽命中区域样式
- [x] 2.4 在 `styles.css` 为三种工作区模式补充显隐规则

## 3. 拖拽分割线交互（桌面端）

- [x] 3.1 实现 `mousedown` 启动拖拽、`mousemove` 更新临时分割位置
- [x] 3.2 实现 `mouseup` 提交最终比例并结束拖拽状态
- [x] 3.3 在拖拽过程中禁止触发完整预览重渲染
- [x] 3.4 在拖拽结束后仅触发一次 `schedulePreviewUpdate()`
- [x] 3.5 增加窗口级 `mouseup` 兜底，避免拖拽状态卡死

## 4. 最小宽度阈值与自动模式切换

- [x] 4.1 定义编辑区/预览区最小宽度阈值常量（如 280/320）
- [x] 4.2 在拖拽提交时判断阈值越界并自动切换到 `editor-only` 或 `preview-only`
- [x] 4.3 从单栏切回 `split` 时恢复最近一次有效比例
- [x] 4.4 刷新后验证比例回默认值（不从 localStorage 恢复）

## 5. 端上策略与验证

- [ ] 5.1 桌面端验证分割线可拖拽、松手后更新生效
- [ ] 5.2 移动端验证不启用拖拽，仅保留模式切换
- [ ] 5.3 验证模式切换不会破坏现有同步滚动与预览可用性
- [ ] 5.4 手动验证导出、打印、图表配置等现有功能未回归
