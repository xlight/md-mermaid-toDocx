## 1. 同步滚动架构重构

- [x] 1.1 将现有同步滚动拆分为“映射层”和“运动控制层”
- [x] 1.2 保留百分比同步作为兜底路径，确保任何时候可降级
- [x] 1.3 定义统一同步入口，支持编辑区与预览区双向调用

## 2. 分段映射与锚点索引

- [x] 2.1 基于渲染段落构建结构锚点索引（sourcePos <-> targetPos）
- [x] 2.2 实现分段单调插值映射，替代全局单比例映射
- [x] 2.3 在预览重建完成后重建锚点索引
- [x] 2.4 索引缺失或异常时自动回退到百分比同步

## 3. 文本锚点微调

- [x] 3.1 提取顶部可见文本特征（归一化）作为候选锚点
- [x] 3.2 实现目标侧邻域窗口匹配，避免全局误命中
- [x] 3.3 引入匹配置信度阈值，仅高置信度时应用微调
- [x] 3.4 匹配歧义时跳过文本修正并回退到结构/百分比结果

## 4. 抗跳动控制

- [x] 4.1 实现死区阈值（小位移忽略）
- [x] 4.2 实现单帧位移限幅（防止瞬移）
- [x] 4.3 实现平滑逼近策略（动态 alpha）
- [x] 4.4 实现锚点切换滞回（防边界抖动）
- [x] 4.5 实现方向锁与短时冷却（防双向互拉）

## 5. 参数调优与不确定项收敛

- [x] 5.1 确定并集中管理关键参数初始值（死区、限幅、窗口、阈值）
- [x] 5.2 基于真实文档样本调优默认参数（含图表密集、公式密集、长文本场景）
- [x] 5.3 记录“参数调优决策与理由”到变更文档，便于后续迭代

## 6. 验证与回归

- [x] 6.1 手动验证编辑区 -> 预览区同步稳定性
- [x] 6.2 手动验证预览区 -> 编辑区同步稳定性（修复：取消注释 syncFromPreview，恢复双向同步）
- [x] 6.3 验证在 Mermaid/MathJax 异步渲染后同步不出现大幅跳动
- [x] 6.4 验证现有导出、打印、主题与模式切换功能无回归
