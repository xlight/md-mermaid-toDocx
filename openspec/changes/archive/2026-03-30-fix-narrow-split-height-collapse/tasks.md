## 1. 窄屏 split 高度分配规则

- [x] 1.1 在 `styles.css` 为窄屏 `split` 布局补充编辑区/预览区的弹性高度分配规则
- [x] 1.2 为编辑区与预览区添加最小高度约束，确保任一侧不会被压缩到不可用
- [x] 1.3 验证超长内容时两侧通过内部滚动承载而非互相挤压

## 2. 极短视口兜底与模式稳定性

- [x] 2.1 在 `app.js` 的视图模式/resize 流程中加入极短视口兜底判定
- [x] 2.2 当可用高度不足时先应用紧急最小高度策略，仍不足则自动切换单栏模式
- [x] 2.3 确认窄屏策略不影响桌面端（>=1024px）横向拖拽分栏逻辑

## 3. 手动验证与回归检查

- [ ] 3.1 在 320/375/390/414 宽度下验证 split 模式不再出现编辑区持续塌陷
- [ ] 3.2 验证软键盘弹出与收起时布局不会频繁抖动或进入不可用状态
- [ ] 3.3 回归验证 `split` / `editor-only` / `preview-only` 切换行为与工具栏窄屏模式
